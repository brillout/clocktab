{
  "scripts": {
    "dev": "serve src/",
    "deploy": "yarn prod:build && push-dir --dir=dist/ --branch=gh-pages",
    "prod": "yarn prod:build && yarn prod:serve",
    "prod:build": "rm -rf dist/ && parcel build src/index.html --public-url '.'",
    "prod:serve": "serve dist/"
  },
  "dependencies": {
    "parcel": "^1.12.4",
    "push-dir": "^0.4.1",
    "serve": "^11.3.0"
  }
}
